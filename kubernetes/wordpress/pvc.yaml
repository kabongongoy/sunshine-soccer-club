apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wordpress-pvc
  namespace: soccer
spec:
  storageClassName: nfs-readynas
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
